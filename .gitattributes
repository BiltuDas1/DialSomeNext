# Set default behavior to automatically normalize line endings to LF
* text=auto eol=lf

# Force specific Windows files to keep CRLF (Important for gradlew.bat!)
*.bat text eol=crlf
*.cmd text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.gif binary
*.jar binary